export interface SeoData {
  title: string;
  description: string;
  keywords?: string;
  canonicalUrl?: string /*Canonical URL (optional) */;
  ogImage?: string /* Open Graph image for social media (optional) */;
}
